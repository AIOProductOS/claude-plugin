---
description: Run Monday-morning product triage on your connected AIOProductOS workspace
---

Run this week's product triage on the connected AIOProductOS workspace.

Follow the `aioproductos-triage` skill exactly — load it with the Skill tool before doing anything else. In short: ground in the product brain, surface the top 3 customer-evidenced insights, score them transparently from real revenue and usage (Effort stays the user's call), present the table and WAIT for confirmation, then create the tasks, assign a named AI teammate, and schedule them into the next sprint.

If the AIOProductOS MCP tools (e.g. `get_product_brain`, `list_insights`, `create_task`) are not available in this session, stop and tell the user to connect the workspace first: Settings → Connect & Tokens → "Connect to Claude", or custom connector URL `https://platform.aioproductos.com/api/mcp`. Do not fabricate a triage without the connector.
